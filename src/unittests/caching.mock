/* -*- c -*-*/
#include "../caching.h"
#include <cgreen/mocks.h>

S_caching s_cache;

int checkFileModifiedTime(int ii) {
  return (int) mock(ii);
}

void initCaching(void) {
  mock();
}

void recoverCachePoint(int i, char *readedUntil, int activeCaching) {
  mock(i, readedUntil, activeCaching);
}

void recoverFromCache(void) {
  mock();
}

void cacheInput(void) {
  mock();
}

void cacheInclude(int fileNum) {
  mock(fileNum);
}

void placeCachePoint(int inputCacheFlag) {
  mock(inputCacheFlag);
}

void recoverCachePointZero(void) {
  mock();
}

void recoverMemoriesAfterOverflow(char *cxMemFreeBase) {
  mock(cxMemFreeBase);
}
