/* -*- c -*-*/
#include "yacc_parser.x"
#include <cgreen/mocks.h>

int yaccyyparse() {
  return (int) mock();
}

void makeYaccCompletions(char *s, int len, Position *pos) {
     (void)mock(s, len, pos);
}
