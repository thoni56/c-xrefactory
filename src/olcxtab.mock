/* -*- c -*-*/
#include "olcxtab.h"
#include <cgreen/mocks.h>


SessionData *currentUserData;


void olcxTabInit (struct olcxTab *table, int size) {
    mock(table, size);
}

void olcxTabNoAllocInit(struct olcxTab *table, int size) {
    mock(table, size);
}

bool olcxTabIsMember(struct olcxTab *table, SessionData *element, int *position, SessionData **origin) {
    return (int)mock(table, element, position, origin);
}

bool olcxTabNextMember(SessionData *element, SessionData **original) {
    return (bool)mock(element, original);
}

void olcxTabSet(struct olcxTab *table, SessionData *element, int position) {
    mock(table, element, position);
}

void olcxTabSetLast(struct olcxTab *table, SessionData *element, int position) {
    mock(table, element, position);
}

int olcxTabAdd(struct olcxTab *table, SessionData *element) {
    return mock(table, element);
}

bool olcxTabDelete(struct olcxTab *table, SessionData *element) {
    return (bool)mock(table, element);
}

bool olcxTabDeleteExact(struct olcxTab *table, SessionData *element) {
    return (bool)mock(table, element);
}

int olcxTabDeleteLast(struct olcxTab *table, SessionData *element) {
    return (int)mock(table, element);
}

void olcxTabMap(struct olcxTab *table, void (*fun)(SessionData *)) {
    mock(table, fun);
}

void olcxTabMap2(struct olcxTab *table, void (*fun)(SessionData *, void *), void *a) {
    mock(table, fun);
}

void olcxTabMap3(struct olcxTab *table, void (*fun)(int)) {
    mock(table, fun);
}

void olcxTabMap5(struct olcxTab *table, void (*fun)(SessionData *, void *), void *arg) {
    mock(table, fun, arg);
}

void olcxTabStatistics(struct olcxTab *table, int *usedIndexes, int *elements, int *maxdepth) {
    mock(table, usedIndexes, elements, maxdepth);
}
