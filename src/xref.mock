#include <cgreen/mocks.h>

#include "xref.h"

void checkExactPositionUpdate(bool printMessage) {
    mock(printMessage);
}

void callXref(ArgumentsVector args, bool isRefactoring) {
    mock(args.argc, args.argv, isRefactoring);
}

void xref(ArgumentsVector args) {
    mock(args.argc, args.argv);
}
