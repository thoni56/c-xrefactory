/* -*- c -*-*/
#include <cgreen/mocks.h>

#include "lexer.h"


bool buildLexemFromCharacters(CharacterBuffer *cb, LexemBuffer *buffer) {
  return (bool) mock(buffer);
}

void gotOnLineCxRefs(Position pos) {
    Position *position = &pos;
    mock(position);
}
