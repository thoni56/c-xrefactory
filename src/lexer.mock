/* -*- c -*-*/
#include "lexer.h"
#include <cgreen/mocks.h>

bool getLexemFromLexer(LexemBuffer *buffer) {
  return (bool) mock(buffer);
}

void gotOnLineCxRefs(Position *position) {
  mock(position);
}
