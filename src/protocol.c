/*
  This will auto generate initialized declarations of protocol items
  from protocol.h and protocol.tc. If "PROTOCOL_C" is set the
  result will be declarations, which are used here.
*/
#define PROTOCOL_C

#include "protocol.h"
