/* -*- c -*-*/
#include "c_parser.h"
#include <cgreen/mocks.h>


const char *operationNamesTable[] = {
};

void initServer(ArgumentsVector args) {
    mock(args.argc, args.argv);
}

void callServer(ArgumentsVector args, ArgumentsVector nargs) {
  mock(args.argc, args.argv, nargs.argc, nargs.argv);
}

void server(ArgumentsVector args) {
    mock(args.argc, args.argv);
}
