/* -*- c -*-*/
#include "c_parser.h"
#include <cgreen/mocks.h>

void initServer(ArgumentsVector args) {
    mock(args.argc, args.argv);
}

void callServer(ArgumentsVector args, ArgumentsVector nargs, bool *firstPass) {
  mock(args.argc, args.argv, nargs.argc, nargs.argv, firstPass);
}

void server(ArgumentsVector args) {
    mock(args.argc, args.argv);
}
