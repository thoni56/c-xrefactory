/* -*- c -*-*/
#include "classhierarchy.h"
#include <cgreen/mocks.h>

ClassHierarchyReference *newClassHierarchyReference(int origin, int superClass, ClassHierarchyReference *next) {
    return (ClassHierarchyReference *)mock(origin, superClass, next);
}

void initClassHierarchyGeneration(void) {
  mock();
}

void setTmpClassBackPointersToMenu(SymbolsMenu *menu) {
  mock(menu);
}

void splitMenuPerSymbolsAndMap(SymbolsMenu *menu, void (*fun)(SymbolsMenu *, void *, void *), void *p1, char *p2) {
  mock(menu, fun, p1, p2);
}

void generateGlobalReferenceLists(SymbolsMenu *menu, FILE *file, char *fn) {
  mock(menu, file, fn);
}

void genClassHierarchies(SymbolsMenu *menu, FILE *file, int pass) {
  mock(file, menu, pass);
}

bool isSmallerOrEqClass(int inferior, int superior) {
    return (bool) mock(inferior, superior);
}

int classCmp(int class1, int class2) {
    return (int)mock(class1, class2);
}

bool classHierarchyClassNameLess(int classname1, int classname2) {
  return (int) mock(classname1, classname2);
}
