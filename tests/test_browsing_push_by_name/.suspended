Missing: decided behaviour for push by name when the name has several bindings

browsing.c has three bindings of 'i': the struct field at 10:22, the local at
13:8 and another in the inner block at line 22. -olcxpushname=i answers
<display-or-update-browser> and browses the local.

The expected here says it jumps to the struct field. That was recorded when
this Makefile ran XrefMode -create first, and the on-disk database holds only
externals, so the locals did not exist and the jump was unique by accident.
The -create is gone now and the database is cleared on every run, so this is a
real cold start.

Decide what an ambiguous name should do, then rewrite expected from the second
push onward. Everything before it is verified, including the "1 of 1
compilation units need reparsing. Parse all before browsing?" prompt that
arrives now that push by name is in the parse-all gate in src/server.c.
